#! /bin/sh -x

/home/{{ elk_user }}/stopElasticSearch.sh

export JAVA_HOME={{ java_home }}
export PATH={{ java_home }}/bin:$PATH
export ES_HEAP_SIZE=2g

nohup {{ elastic_home }}/bin/elasticsearch > {{ elastic_home }}/nohup.out 2>&1 &
tail -f {{ elastic_home }}/nohup.out
